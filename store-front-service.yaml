apiVersion: v1
kind: Service
metadata:
  name: store-front
  namespace: bestbuy
spec:
  selector:
    app: store-front
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer